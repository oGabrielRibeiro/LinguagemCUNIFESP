#include <stdio.h>

int main() {

     double B;
    int A,i;
    for(i=1;i<=6;i++)
    {
    scanf("%lf",&B);
        if(B>=0)
        {   
        A=A+1;
        }
    }
    printf("%d valores positivos\n",A);

    return 0;
}
